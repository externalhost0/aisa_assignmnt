# SHOWS WHATS NEEDED, NEVER ACTUALLY USED ANYWHERE
# Anthropic API key — get one at console.anthropic.com
ANTHROPIC_KEY=sk-ant-...

# Database URL — defaults to SQLite, swap for Postgres in production
# DATABASE_URL=postgresql://user:password@host:5432/campus_safety
